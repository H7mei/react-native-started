import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const HomeScreen = () => {
  return (
    <SafeAreaView>
      <Text className="text-white text-2xl bg-red-400">HomeScreen</Text>
    </SafeAreaView>
  );
};

export default HomeScreen;
